{
  "name": "workshop-root",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix marshmallow && npm install --prefix split",
    "build": "node scripts/build-all.js",
    "start": "node scripts/serve.js",
    "serve": "node scripts/serve.js",
    "deploy": "node scripts/build-all.js && npx firebase-tools deploy --only hosting"
  }
}
